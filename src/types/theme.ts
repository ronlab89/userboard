// Props for the ToggleTheme component controlling position styles
export type ToggleThemeProps = {
  position: string; // CSS position (e.g., "absolute", "relative")
  right: string; // CSS right position (e.g., "right-0")
  top: string; // CSS top position (e.g., "top-0")
};
